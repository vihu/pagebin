services:
  pagebin:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:${PAGEBIN_PORT:-5050}:${PAGEBIN_PORT:-5050}"
    volumes:
      - pagebin-data:/data

volumes:
  pagebin-data:
